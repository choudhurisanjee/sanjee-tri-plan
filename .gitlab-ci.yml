pages:
  stage: deploy
  image: alpine:latest
  script:
    - mkdir -p public
    - cp triathlon-plan.html public/index.html
    - cp triathlon-plan.html public/triathlon-plan.html
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
